import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.iutblagnac.stdsapp',
  appName: 'STDS App',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
